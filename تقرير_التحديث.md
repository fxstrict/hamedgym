# تقرير تحديث SEO و OG — Hamed Gym for Ladies

## السبب الحقيقي وراء عدم ظهور الصورة
كل وسوم `og:image` و `og:url` و `canonical` كانت متربطة بـ placeholder اسمه `SITE_URL` بدل رابط الموقع الحقيقي (لأن الموقع مكانش منشور وقت التسليم الأول). يعني فيسبوك وواتساب كانوا بيحاولوا يجيبوا صورة من رابط وهمي مش موجود، فمش بيعرضوا حاجة — **الكود نفسه كان سليم، بس الرابط جواه كان placeholder**.

**اتصلح دلوقتي بالكامل** وربطت كل الوسوم برابطك الفعلي:
```
https://fxstrict.github.io/hamedgym/
```
وتأكدت بنفسي إن صورة الـ OG شغالة ومنشورة فعليًا على الرابط (فحصتها مباشرة ورجعتلي HTTP 200 + Content-Type: image/jpeg).

---

## ليه المشكلة دي بتتكرر معاكِ عمومًا مع أي صفحة/موقع/تطبيق؟
في 3 أسباب شائعة، خصوصًا لو الموقع اتسلم كملف مضغوط قبل ما يترفع:

1. **رابط الصورة "نسبي" مش "مطلق"** — لازم `og:image` يبدأ بـ `https://` وياخد الدومين كامل، مش مسار زي `images/og.jpg` بس. فيسبوك وواتساب بيرفضوا أي رابط ناقص.
2. **الموقع لسه مش منشور وقت إضافة الوسوم** — أي موقع بيتسلم كملفات محلية، الدومين النهائي بيبقى مش معروف وقت التسليم، فلازم يتحدث لاحقًا بالرابط الحقيقي بعد الرفع (زي اللي حصل هنا بالظبط).
3. **كاش فيسبوك/واتساب القديم** — لو قبل كده بعتِ الرابط من غير صورة، فيسبوك بيحتفظ بأول نسخة شافها لفترة، ولازم "تجبريه" ياخد نسخة جديدة (الخطوة التالية).

**نصيحة لأي مرة جاية:** لو الموقع لسه مش منشور، قوليلي الرابط النهائي المتوقع من الأول (حتى لو GitHub Pages بمعاينة تقديرية)، عشان أظبط الوسوم بيه من البداية بدل placeholder.

---

## الخطوة الوحيدة المتبقية منك: إجبار فيسبوك وواتساب يحدّثوا الكاش
واتساب وفيسبوك بيحتفظوا بنسخة قديمة من بيانات الرابط أول ما حد يبعته، فحتى بعد التصحيح، المعاينة القديمة (من غير صورة) ممكن تفضل ظاهرة لحد ما "تجبريهم" ياخدوا نسخة جديدة:

1. افتحي: **https://developers.facebook.com/tools/debug/**
2. حطي الرابط: `https://fxstrict.github.io/hamedgym/`
3. دوسي **"Scrape Again"** (أو "Debug" لو أول مرة) — ده بيمسح الكاش القديم ويجيب النسخة الجديدة فورًا، وهتشوفي صورة المعاينة ظاهرة في نتيجة الفحص.
4. بعد كده جربي تبعتي الرابط في واتساب لنفسك — المعاينة هتظهر صح.

لتويتر/X: **https://cards-dev.twitter.com/validator** (نفس الخطوة).

> ملحوظة: لو بعتِ الرابط في واتساب قبل الخطوة دي، ممكن يفضل شكل قديم عند اللي استلموا الرسالة القديمة بس — الرسائل الجديدة هتظهر صح.

---

## ملخص كل التعديلات (للمرجعية)
- صورة OG احترافية 1200×630 من صور الجيم الحقيقية: `images/og/og-image.jpg`
- عنوان ووصف تسويقي يجمع الجيم + المنتجات لكل من فيسبوك وتويتر وواتساب
- `canonical` + `og:url` + `twitter:url` مربوطين بالرابط الحقيقي
- `robots.txt` و `sitemap.xml` بمسار صحيح
- تحسين JSON-LD (schema.org) بإضافة `image` و `url` و `priceRange`
- أيقونات PNG احترافية (`apple-touch-icon.png`, `icon-512.png`)

كل الملفات دي جاهزة ومربوطة بالدومين الصح في الحزمة المرفقة.
