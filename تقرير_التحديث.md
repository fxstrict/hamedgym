# تقرير تحديث SEO و OG — Hamed Gym for Ladies

## ⚠️ خطوة واحدة لازم تعمليها قبل النشر
كل الروابط الجديدة فيها placeholder اسمه `SITE_URL` بدل الدومين الحقيقي، لأن الموقع لسه مش مرفوع على دومين ثابت.
بعد ما ترفعي الموقع (GitHub Pages / Vercel / Netlify أو دومين خاص):

1. افتحي `index.html` و `robots.txt` و `sitemap.xml`
2. استخدمي **Find & Replace** واستبدلي `SITE_URL` بدومين موقعك الفعلي، مثلاً:
   - لو الدومين `hamedgym.com` → استبدليها بـ `hamedgym.com`
   - لو GitHub Pages → استبدليها بـ `username.github.io/repo-name`
3. لازم الرابط يبدأ بـ `https://` (موجود بالفعل في كل مكان، بس لو غيرتيه تأكدي إنه فضل زي ما هو)

**من غير الخطوة دي، صورة المعاينة مش هتظهر في فيسبوك/واتساب/تويتر**، لأن المواقع دي بتطلب رابط صورة "مطلق" (كامل) مش رابط نسبي.

---

## 1) صورة Open Graph (OG Image) الاحترافية
- اتعملت صورة مخصوصة بمقاس **1200×630px** (المقاس القياسي المعتمد من فيسبوك وواتساب ولينكدإن) من صورة حقيقية من جيمكم (`images/gym/gym-13.jpg`) بتظهر شعار "HAMED GYM for ladies" بوضوح مع أجواء الحصة الرياضية (زومبا) والهوية اللونية الفوشيا/الذهبي المميزة للبراند.
- الصورة الجديدة محفوظة في: `images/og/og-image.jpg`
- ربطتها في وسوم `og:image` و `twitter:image` و `schema.org` (JSON-LD) عشان تظهر في:
  - **فيسبوك** (منشور، ماسنجر)
  - **واتساب** (لما تبعتي رابط الموقع في شات أو حالة)
  - **تويتر / X** (بطاقة `summary_large_image` — أكبر حجم متاح للمعاينة)
  - **لينكدإن، تيليجرام، وأي منصة بتقرأ Open Graph**

## 2) نص تسويقي جذاب للمشاركة (Title + Description)
اتكتب نص احترافي يجمع بين هوية الجيم والمنتجات، يظهر تلقائيًا تحت الصورة عند المشاركة:

> **العنوان:** Hamed Gym for Ladies | جيم حامد للسيدات
> **الوصف:** جسمك الجديد يبدأ من هنا 💪 تدريب مقاومة وزومبا ورقص شرقي لتنسيق قوامك، مع بروتين ومكملات تخسيس أصلية 100%. اشتركي الآن أو اطلبي منتجاتك عبر واتساب.

ده بيغطي في جملة واحدة: الجيم (تدريب/زومبا/رقص شرقي) + المنتجات (بروتين/تخسيس) + دعوة لإجراء (اشتركي/اطلبي).

## 3) وسوم SEO الأساسية المضافة
| الوسم | الفايدة |
|---|---|
| `<link rel="canonical">` | يمنع مشاكل تكرار المحتوى في جوجل |
| `<meta name="robots" content="index, follow, max-image-preview:large">` | يسمح لجوجل بفهرسة الصفحة وعرض صور كبيرة في نتائج البحث |
| `<meta name="keywords">` | كلمات مفتاحية متعلقة بالمجال (جيم سيدات، تخسيس، بروتين...) |
| `og:url` / `twitter:url` | يحدد الرابط الرسمي الظاهر عند المشاركة |
| `og:image:width/height/alt` | يضمن ظهور الصورة بالحجم الصحيح فورًا بدون تأخير من فيسبوك |
| تحسين JSON-LD (`schema.org`) | إضافة `image` و `url` و `priceRange` لظهور أفضل في نتائج البحث المحلي بجوجل (Google Maps / Local Pack) |
| `robots.txt` + `sitemap.xml` | ملف Sitemap جديد يساعد جوجل يكتشف الصفحة ويفهرسها أسرع |
| `apple-touch-icon.png` | أيقونة احترافية (PNG حقيقي) تظهر لو حد ضاف الموقع على شاشة آيفون الرئيسية |

## 4) ملفات جديدة اتضافت
```
images/og/og-image.jpg       ← صورة المعاينة عند المشاركة (1200×630)
images/apple-touch-icon.png  ← أيقونة للموبايل (180×180)
images/icon-512.png          ← أيقونة عالية الدقة لملف PWA (512×512)
sitemap.xml                  ← خريطة الموقع لمحركات البحث
```

## 5) كيف تتأكدي إن كل حاجة شغالة بعد النشر
بعد رفع الموقع واستبدال `SITE_URL`، اختبري الرابط في الأدوات دي (مجانية):
- **فيسبوك/واتساب:** https://developers.facebook.com/tools/debug/ → حطي رابط موقعك واضغطي "Scrape Again"
- **تويتر/X:** https://cards-dev.twitter.com/validator (أو جربي بعت الرابط لنفسك في تويتر مباشرة)
- **لينكدإن:** https://www.linkedin.com/post-inspector/

لو الصورة القديمة ظهرت بدل الجديدة، ده بسبب الكاش عند فيسبوك — استخدمي زرار "Scrape Again" في أداة الديباج بتاعتهم وهتتحدث فورًا.

## ملاحظة
لو حبيتي تغيّري صورة الـ OG لصورة تانية من صور الجيم أو المنتجات، وريني الصورة اللي عايزاها وهعملها بنفس المقاس المضبوط (1200×630) فورًا.
